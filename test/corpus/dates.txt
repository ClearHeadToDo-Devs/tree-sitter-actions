============
with do-date
============
(x) context test @2019-01-01T12:01AMD120

---

(action_list
 (root_action
  (core_action
   (state 
    (completed))
   (name)
   (do_date_or_time
    (do_date_icon)
    (extended_date_and_time
     (date_and_time
      (date
       (year)
       (month)
       (day))
      (time
       (time_designator)
       (hour)
       (minute)))
     (duration
      (duration_designator)
      (duration_value)))))))

=======================
with recurring do-date
=======================
(x) context test @2019-01-01T12:01AMD120RDaT01:05PM

---

(action_list
 (root_action
  (core_action
   (state 
    (completed))
   (name)
   (do_date_or_time
    (do_date_icon)
    (extended_date_and_time
     (date_and_time
      (date
       (year)
       (month)
       (day))
      (time
       (time_designator)
       (hour)
       (minute)))
     (duration
      (duration_designator)
      (duration_value)))
    (recurrance
     (recurrance_icon)
     (recurrance_structure
      (daily_recurrance
       (time
	(time_designator)
	(hour)
	(minute)))))))))

====================
with completed date
====================
(x) context test %2019-01-01T12:01AM

---


(action_list
 (root_action
  (core_action
   (state 
    (completed))
   (name)
   (completed_date
   (date_and_time
    (date
     (year)
     (month)
     (day))
     (time
      (time_designator)
      (hour)
      (minute)))))))
