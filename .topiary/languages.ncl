{
  languages = {
    actions = {
      extensions | default = ["actions"],
      grammar = {
        source | default = {
          path = "/home/backup-admin/Products/tree-sitter-actions/actions.so",
        },
      },
    },
  },
}
