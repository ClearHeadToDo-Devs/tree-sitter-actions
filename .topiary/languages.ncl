{
  languages = {
    actions = {
      extensions = ["actions"],
      grammar = {
        source = {
          path = "/home/backup-admin/Products/platform/tree-sitter-actions/actions.so",
        },
      },
    },
  },
}
